# Test configuration

# Hibernate & JPA configuration
spring.jpa.hibernate.ddl-auto=create-drop

# Disable data.sql during tests
spring.sql.init.mode=never

spring.jpa.show-sql=true
spring.jpa.defer-datasource-initialization=true